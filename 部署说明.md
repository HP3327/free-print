# 免费打印 H5 · GitHub Pages 部署说明

> 本文件夹是「免费打印」推文的网页版（扁平结构，可直接上传）。
> 部署平台：**GitHub Pages**（你之前预约打印用的同一套 `*.github.io`）。

## 一、准备仓库（github.com）
1. 登录 github.com，新建仓库，仓库名自取（如 `free-print`），**公开（Public）**。
2. 进入仓库 → 把本文件夹**全部 6 个文件直接拖到仓库根目录**上传：
   `index.html`、`img0.jpg`、`img1.jpg`、`img2.jpg`、`img3.jpg`、`.nojekyll`
   - 本文件夹已是扁平结构（无子目录），网页上传**不会被拍平**，路径不会错。
   - 若用 git 推送，同样把这几个文件放在仓库根：
     `git add . && git commit -m "free-print h5" && git push -u origin main`

## 二、开启 GitHub Pages
1. 仓库页 → **Settings**（右上角）→ 左侧 **Pages**。
2. **Build and deployment** → Source 选 `Deploy from a branch`。
3. Branch 选你上传的分支（`main` 或 `master`），目录选 `/ (root)`，点 **Save**。
4. 等 1–2 分钟，页面顶部会出现绿色提示，生成网址形如：
   `https://<你的用户名>.github.io/free-print/`
   → 这就是可放进微信菜单 / 自动回复的**永久链接**。

## 三、接入微信菜单 / 自动回复
- **业务域名**：本页无输入框、无微信 JS-SDK，**无需配置**微信业务域名，直接放链接即可打开。
- **菜单**：在云瞻 / 公众号后台，把「免费打印」菜单指向该 `github.io` 链接。
- **关键词自动回复（推荐）**：设关键词「免费打印」→ 回复文字「简单三步免费打印：<链接>」，用户点开即达，绕过云瞻仅能引草稿的限制。

## 四、本地自测（上传前建议）
双击 `index.html` 用浏览器打开，确认：
- 页面正常渲染、顶部导航可跳转；
- 四张图都能点开「放大」；
- 客服名片（img3）长按可识别添加。

## 五、已知注意（不阻塞，心里有数）
- 国内访客打开 `*.github.io` 偶尔偏慢或间歇打不开（GitHub 境外服务器、未做国内加速）。
  你之前已把预约打印也部署在 GitHub，保持同一平台、管理统一；若日后发现学生端打不开比例高，再换 Gitee Pages（免费、国内稳）即可，文件结构无需改动。
- `.nojekyll` 作用：让 Pages 不忽略带点文件、不做 Jekyll 处理，确保图片与配置原样生效，务必一起上传。

---
文件清单：
- index.html — 页面主体（橙色主题 / 吸顶导航 / 图片点击放大 / 客服名片长按识别）
- img0.jpg — 对话框发截图示例
- img1.jpg — 消费截图示例
- img2.jpg — 打印支付截图
- img3.jpg — 专属客服微信名片
- .nojekyll — 让 Pages 不忽略带点文件
